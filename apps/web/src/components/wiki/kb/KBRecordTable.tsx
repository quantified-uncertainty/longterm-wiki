/**
 * KBRecordTable -- STUB (records infrastructure removed).
 *
 * Records have been migrated to PostgreSQL. This component is kept as a no-op
 * stub so existing MDX pages that reference it don't break at build time.
 */

export function KBRecordTable(_props: {
  entity?: string;
  collection?: string;
  columns?: string[];
  [key: string]: unknown;
}): React.ReactElement | null {
  return null;
}
