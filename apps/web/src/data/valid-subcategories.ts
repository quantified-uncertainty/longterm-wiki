// Re-export of the canonical subcategory list in crux/lib so the frontmatter
// validator and the UI type never drift (QUA-212). Do not inline — edit the
// canonical file at crux/lib/valid-subcategories.ts instead.
export { VALID_SUBCATEGORIES, type ValidSubcategory } from "../../../../crux/lib/valid-subcategories";
