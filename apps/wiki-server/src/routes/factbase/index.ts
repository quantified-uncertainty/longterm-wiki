/**
 * FactBase routes — structured triples with temporal data and provenance.
 *
 * Covers: facts CRUD. Source-checks are in the unified /api/sourcing route.
 */
export { factsRoute, type FactsRoute } from "./facts.js";
