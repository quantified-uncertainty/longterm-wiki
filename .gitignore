# ad-hoc SQL scripts
dev/sql/
dev/reports/

# build output
dist/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production
.env*.local

# macOS
.DS_Store

# Next.js
.next/
apps/web/out/
apps/web/next-env.d.ts
apps/web/tsconfig.tsbuildinfo

# Generated data (all rebuilt by prebuild script / build-data.mjs)
src/data/
apps/web/src/data/*.json
apps/web/src/data/pg-audit-diff.md
apps/web/src/data/factbase-audit-diff.md
public/search-docs.json
public/search-index.json
data/id-registry.json

# Auto-generated LLM accessibility files
apps/web/public/llms*.txt
apps/web/public/wiki/
apps/web/public/search-docs.json
apps/web/public/search-index.json

# Test results (Playwright / Vitest artifacts)
test-results/
/coverage/
apps/web/coverage/
apps/wiki-server/coverage/
apps/discord-bot/coverage/
crux/coverage/

# Knowledge cache
.cache/

# Agent workspace slot marker (per-clone, written by `crux agent-workspace setup`)
.agent-slot

# Per-slot dispatch state: run dirs, events.jsonl, meta.json (QUA-554).
# Contains prompts + cost data; not checked in.
.dispatch/

# Claude Code temp files
.claude/temp/
.claude/plan.md
.claude/settings.local.json
.claude/maintain-last-run.txt
.claude/wip-checklist.md
.claude/wip-context.md
.claude/plan.md
.claude/issue-creates.json
.claude/agent-id
.claude/active-branch
.claude/session.pid
.claude/last-heartbeat
.claude/review-done
.claude/review-phases-done
.claude/simplify-done
.claude/worktree-cleanup.log
.claude/worktrees/
.claude/backups/
.claude/scheduled_tasks.lock

# MCP config (contains tokens)
.mcp.json

# Serena MCP server local config (cache + project.local.yml)
.serena/

# Session logs (operational state — stored in PostgreSQL via wiki-server)
.claude/sessions/

# Vercel
.vercel

# pnpm
.pnpm-store/

# Mermaid validation temp
.mermaid-validate-temp/

# Auto-update operational state (stored in PostgreSQL via wiki-server)
data/auto-update/state.yaml
data/auto-update/runs/

# Citation accuracy results (operational audit data — stored in PostgreSQL)
data/citation-accuracy/

# Resource snapshot (operational cache, regenerated by `crux wiki-server snapshot-resources`)
data/resources-snapshot.json

# Derived fact ID mapping (generated by migrate-fact-ids.mjs)
scripts/fact-id-mapping.json

# Eval results (ephemeral — regenerated on each run)
crux/evals/results/

# Semantic diff snapshots (audit trail — not source code, stored locally)
.claude/snapshots/

# QA sweep result history (local trend data — not source code)
.claude/sweep-results/

# Matrix score snapshots (local analysis, not source code)
.matrix-snapshots/

# Design docs — committed for review. Gitignore removed intentionally.
.matrix-improved.txt
.playwright-mcp/
crux/scripts/career-data.json

# Archived data exports (large JSON files that bloat the repo)
data/archived/
data/archive/

# Calibration raw data — regenerable via crux/calibration/run-calibration.ts
# Only metrics.json + audit doc are committed; corpus + per-item results rebuild on demand
crux/calibration/data/corpus.json
crux/calibration/data/results-*.json
.claude/scratch/
.claude/research-cache/
