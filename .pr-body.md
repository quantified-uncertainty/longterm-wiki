## Summary

Follow-up to PR #4858 (QUA-1049). Per QUA-1043, the QUA-943 v4/v5 plans were rejected. PR #4858 deleted the `bestEffortAllowed` opt-in sync mode (~941 LOC), but several Phase 0/1 scaffolds had been built before the rejection and remained orphaned. This PR deletes them.

**Net: 6 files changed, +10 / -1210.**

## Removed

| File | Lines | Why dead |
|---|---|---|
| `crux/lib/wiki-server/policy-stakeholders.ts` | 167 | QUA-957 typed client. Its own docstring already acknowledged: *"No production caller invokes this… Phase 2 will be the first caller."* Phase 2 = cancelled (QUA-958). |
| `crux/lib/research/sync-applied.ts` | 206 | QUA-957 dual-write payload converter. Only its own test imported it. |
| `crux/lib/research/__tests__/sync-applied.test.ts` | 446 | Tests for the orphan converter. |
| `apps/web/src/data/__tests__/strict-fail-stats.test.ts` | 240 | Tests for QUA-953 counter. |
| `apps/web/src/app/api/internal/strict-fail-stats/route.ts` | 37 | Endpoint with zero callers (no dashboard, no monitoring, no scripts). |
| `apps/web/src/data/tablebase.ts` strict-fail block | ~104 | QUA-953 counter that gated rejected "Phase 6: delete `GenericEntityPassthroughSchema`". `getTypedEntities()` still falls back through `GenericEntityPassthroughSchema` and still warns in dev — only the stats accumulator goes. |

## Out of scope

- **`code:` discriminator (QUA-952)** + its 446-line test — kept as mild error-response polish even without v4 consumers. Can revisit as Tier 2 follow-up.
- **`pipeline_runs` / `withPipelineRun`** — repurposed for cost tracking (QUA-1012/1013), single-instance mutex (QUA-1032), `--inspect` flag (QUA-1034). Active.
- **Natural-key on `policy_stakeholders` (QUA-956)**, **fail-loud `syncPolicyStakeholders` in `build-data` (QUA-964)**, **`validate-entity-schema-drift` (per its own QUA-1043 retcon)** — retained as standalone improvements.

## Verification

- `pnpm tsc --noEmit` clean across `apps/web` and `apps/wiki-server`. Crux baseline unchanged (same pre-existing `playwright-fetcher.ts` + `lifecycle.test.ts` errors as before this PR — fail-open per gate config).
- `pnpm vitest run`: 1698 wiki-server + 2070 web tests pass (zero new failures).
- `pnpm crux w validate gate`: 63/63 passed.
- `grep` confirms zero remaining references to deleted symbols (`STRICT_FAIL*`, `getStrictFailStats`, `StrictFailStats`, `convertAppliedToStakeholderSync`, `getAllPolicyStakeholders`, `getPolicyStakeholdersByPolicy`, `getPolicyStakeholdersByStakeholder`, `deletePolicyStakeholderBatch`). The single surviving reference to `syncPolicyStakeholders` is a comment in `crux/commands/legislation/auto-verify-stakeholders.ts` that points at the surviving (separately-named) `syncPolicyStakeholders` in `build-data.mjs`.

## Test plan

- [x] Wiki-server unit tests pass
- [x] Web unit tests pass
- [x] Gate (63 checks) passes
- [x] TypeScript clean across web + wiki-server
- [ ] Pre-merge CI green
- [ ] PR mergeable to main

Fixes QUA-1102
