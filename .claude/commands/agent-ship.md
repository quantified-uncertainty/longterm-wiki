---
description: Verify the agent checklist is complete, polish the PR, and ship. For sessions without a PR, use /agent-end instead.
effort: medium
---

# Agent Ship

Verify the agent checklist is complete, polish the PR, and ship. For sessions
without a PR, use `/agent-end` instead.

**Read `docs/agent-workflows/agent-ship.md` and execute every step in order.**
That file is the single source of truth and is shared across agent runtimes.
