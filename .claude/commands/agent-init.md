---
description: Initialize an agent checklist and establish working context. Run at session start.
effort: low
---

# Agent Init

Initialize an agent checklist and establish working context.

**Read `docs/agent-workflows/agent-init.md` and execute every step in order.**
That file is the single source of truth and is shared across agent runtimes.
