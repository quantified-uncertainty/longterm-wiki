---
description: Run CI checks locally, push to GitHub, monitor until CI green AND PR mergeable. Fix and retry if anything fails.
effort: medium
---

# Agent Push and Verify

Run CI checks locally, push to GitHub, monitor until CI green AND PR
mergeable. Fix and retry if anything fails.

**Read `docs/agent-workflows/agent-push-and-verify.md` and execute every step in order.**
That file is the single source of truth and is shared across agent runtimes.
