name: Paused Notice
on:
  workflow_call:

jobs:
  paused-notice:
    if: vars.AUTOMATION_PAUSED == 'true'
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::warning::AUTOMATION_PAUSED is set — this workflow was skipped. To resume, run: pnpm crux ci unpause-automation"
