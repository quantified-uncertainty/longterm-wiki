---
name: source-command-agent-end
description: End a Longterm Wiki agent session when no PR is being shipped. Use for research-only, abandoned, maintenance, or already-pushed quick-fix sessions; follows the shared agent-end workflow, updates Linear/GitHub if needed, closes the agent session, and resets the slot safely.
---

# source-command-agent-end

This skill is a thin pointer to the canonical instructions for `agent-end`.

**Read `docs/agent-workflows/agent-end.md` and follow it as written.** That
file is the single source of truth for this command's behaviour and is shared
across all agent runtimes (Claude Code slash command, Codex skill, etc.). Do
not duplicate or paraphrase its content here.
