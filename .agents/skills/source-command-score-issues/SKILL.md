---
name: "source-command-score-issues"
description: "Score open issues with LLM and apply priority/effort/model labels"
---

# source-command-score-issues

This skill is a thin pointer to the canonical instructions for `score-issues`.

**Read `.claude/commands/score-issues.md` and follow it as written.** That file is the single source of truth for this command's behaviour and is shared across all agent runtimes (Claude Code slash command, Codex skill, etc.). Do not duplicate or paraphrase its content here.
