---
name: "source-command-agent-init"
description: "Initialize an agent checklist and establish working context. Run at session start."
---

# source-command-agent-init

This skill is a thin pointer to the canonical instructions for `agent-init`.

**Read `.claude/commands/agent-init.md` and follow it as written.** That file is the single source of truth for this command's behaviour and is shared across all agent runtimes (Claude Code slash command, Codex skill, etc.). Do not duplicate or paraphrase its content here.
