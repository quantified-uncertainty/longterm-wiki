---
name: source-command-agent-ship
description: Verify the agent checklist is complete, polish the PR, and ship. For sessions without a PR, use agent-end instead.
---

# source-command-agent-ship

This skill is a thin pointer to the canonical instructions for `agent-ship`.

**Read `docs/agent-workflows/agent-ship.md` and follow it as written.** That
file is the single source of truth for this command's behaviour and is shared
across all agent runtimes (Claude Code slash command, Codex skill, etc.). Do
not duplicate or paraphrase its content here.
