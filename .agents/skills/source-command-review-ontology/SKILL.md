---
name: "source-command-review-ontology"
description: "Deep analysis of an entity's ontological structure — sub-entities, relationships, statement organization."
---

# source-command-review-ontology

This skill is a thin pointer to the canonical instructions for `review-ontology`.

**Read `.claude/commands/review-ontology.md` and follow it as written.** That file is the single source of truth for this command's behaviour and is shared across all agent runtimes (Claude Code slash command, Codex skill, etc.). Do not duplicate or paraphrase its content here.
