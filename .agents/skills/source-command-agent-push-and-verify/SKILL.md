---
name: "source-command-agent-push-and-verify"
description: "Run CI checks locally, push to GitHub, monitor until CI green AND PR mergeable. Fix and retry if anything fails."
---

# source-command-agent-push-and-verify

This skill is a thin pointer to the canonical instructions for `agent-push-and-verify`.

**Read `.claude/commands/agent-push-and-verify.md` and follow it as written.** That file is the single source of truth for this command's behaviour and is shared across all agent runtimes (Claude Code slash command, Codex skill, etc.). Do not duplicate or paraphrase its content here.
