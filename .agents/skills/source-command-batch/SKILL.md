---
name: "source-command-batch"
description: "Not a real command — redirects to validator-first sweeps or agent slots."
---

# source-command-batch

This skill is a thin pointer to the canonical instructions for `batch`.

**Read `.claude/commands/batch.md` and follow it as written.** That file is the single source of truth for this command's behaviour and is shared across all agent runtimes (Claude Code slash command, Codex skill, etc.). Do not duplicate or paraphrase its content here.
